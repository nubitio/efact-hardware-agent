# efact-hardware-agent — live configuration
# See config.toml.example in this folder for commented reference (not overwritten on save).

port = 8765

# prefer_system_backend = false
# system_printer_name = "POS_D_BASIC_230"

[scale]
enabled = false
protocol = "generic"
baud_rate = 9600
data_bits = 8
parity = "none"
stop_bits = 1
stable_reads = 3
stable_window_ms = 200